enum FailureKind {
  unauthorized,
  forbidden,
  notFound,
  validation,
  conflict,
  rateLimited,
  request,
  network,
  security,
  timeout,
  server,
  cancelled,
  invalidResponse,
  storage,
  unexpected,
}
