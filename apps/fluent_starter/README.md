# Fluent Starter application

Run workspace commands from the repository root. See the [workspace README](../../README.md) for setup, architecture, flavor commands, platform requirements, and tests.

This package owns composition, generated routing, and native runners. Business contracts and feature UI live in workspace packages.
