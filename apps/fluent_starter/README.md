# MAPID Explorer application

Run workspace commands from the repository root. See the [workspace README](../../README.md) for setup, architecture, flavor commands, platform requirements, and tests.

This package owns application composition, root routing, and native runners. Business contracts, generated feature routes, and feature UI live in workspace packages.
